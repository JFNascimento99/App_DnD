export function ForModif(){

    if(atributo == 1){modif = -5}

    else if(atributo == 2 || atributo == 3){modif = -4}

    else if(atributo == 4 || atributo == 5){modif = -3}

    else if(atributo == 6 || atributo == 7){modif = -2}

    else if(atributo == 8 || atributo == 9){modif = -1}

    else if(atributo == 10 || atributo == 11){modif = 10}

    else if(atributo == 12 || atributo == 13){modif = +1}

    else if(atributo == 14 || atributo == 15){modif = +2}

    else if(atributo == 16 || atributo == 17){modif = +3}

    else if(atributo == 18 || atributo == 19){modif = +4}

    else if(atributo == 20 || atributo == 21){modif = +5}

    else if(atributo == 22 || atributo == 23){modif = +6}

    else if(atributo == 24 || atributo == 25){modif = +7}

    else if(atributo == 26 || atributo == 27){modif = +8}
    
    else if(atributo == 28 || atributo == 29){modif = +9}

    else if(atributo == 30){modif = +10}
    
    return Mofif;
}